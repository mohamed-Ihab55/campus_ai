fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Embeddings (Arabic + multilingual)
sentence-transformers==3.0.1

# Vector DB (must match vectorstore format)
chromadb==0.5.3
posthog==3.0.2

# LLM + Reranker HTTP client
# httpx is used for:
#   1. Streaming Ollama responses
#   2. Calling Qwen3-Reranker-0.6B via HuggingFace Inference API
httpx==0.27.0

# NLP utilities
langdetect==1.0.9
langchain-text-splitters==0.2.4
rank-bm25==0.2.2

# Data stack (CRITICAL pins)
numpy==1.26.4
scikit-learn==1.4.2
pandas==2.2.2
joblib>=1.3.0

# Optional but stabilizes HF stack
transformers==4.41.2
tokenizers==0.19.1

# env utils
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# Torch — CUDA 12.1 build for RTX 3050 GPU acceleration
# Install with: pip install torch==2.3.1+cu121 --extra-index-url https://download.pytorch.org/whl/cu121
# If no GPU available, fall back to CPU: pip install torch==2.3.1+cpu
torch==2.3.1+cu121